// Restparameter:  here the function allow the indefinite parameter using ...
// when we parameter get then we use the restparameter
function test(...a) {
  console.log("frends", a);
}
test("dhanu", "pandu", "piu", "chutki", "vas");
